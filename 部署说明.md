# 部署说明

站点已经做好了，本地双击 `index.html` 就能验收。下面是推上公网的步骤。

---

## 发布前必须改的两处

这两处不改就发布，会有实际问题。

### 1. 联系方式（必改）

`index.html` 里搜索这一行，替换成你的联系方式（邮箱 / QQ / 微信 / GitHub Issues 链接都行）：

```
［部署前请把这里替换成你的联系方式］
```

**为什么必须改**：免责声明里承诺了「权利方认为存在不当使用可联系移除」。没有联系方式，这句承诺是空的——而这正是暴雪 Fan Content Policy 下最要紧的一条。

### 2. 域名（必改）

拿到域名之后，在 `site/` 目录下跑：

```bash
./set-domain.sh https://你的域名
```

例如：

```bash
./set-domain.sh https://yourname.github.io/wow-pvp     # GitHub Pages 项目页
./set-domain.sh https://wowpvp.example.com             # 自定义域名
```

脚本会把所有 HTML、`sitemap.xml`、`robots.txt` 里的 `YOUR-DOMAIN` 占位符替换掉，跑完会告诉你有没有残留。

**影响的是**：`canonical` 标签、Open Graph 分享链接、站点地图。不改的话搜索引擎会拿到错误的 URL，收录会出问题。

---

## 方案 A：GitHub Pages（推荐）

免费、你完全掌控、随时能下线、不用备案。

```bash
cd site

# 1. 初始化并提交
git init
git add .
git commit -m "WoW PvP 判断训练器 12.0.7"

# 2. 去 github.com 建一个新仓库（比如叫 wow-pvp），Public
#    然后把下面的 YOURNAME 换成你的用户名
git remote add origin https://github.com/YOURNAME/wow-pvp.git
git branch -M main
git push -u origin main
```

然后在 GitHub 仓库页面：

1. **Settings** → 左侧 **Pages**
2. **Source** 选 `Deploy from a branch`
3. **Branch** 选 `main`，目录选 `/ (root)`
4. **Save**

等一两分钟，站点会出现在 `https://YOURNAME.github.io/wow-pvp/`。

拿到这个地址之后，回来跑一次 `./set-domain.sh https://YOURNAME.github.io/wow-pvp`，然后重新 commit + push。

> `.nojekyll` 文件已经建好了，别删——没有它 GitHub Pages 会忽略某些文件。

---

## 方案 B：Cloudflare Pages（国内访问更快）

同样免费，国内速度通常好于 GitHub Pages。

1. 先按方案 A 把代码推到 GitHub
2. 登录 [dash.cloudflare.com](https://dash.cloudflare.com) → **Workers & Pages** → **Create** → **Pages** → **连接到 Git**
3. 选你刚才那个仓库
4. 构建设置：**Framework preset 选 None**，构建命令留空，输出目录填 `/`
5. **Save and Deploy**

会给你一个 `xxx.pages.dev` 的地址。同样记得回来跑 `set-domain.sh`。

---

## 发布后：让搜索引擎收录

你说了要「真的能被搜到」，所以这一步不能省。

### Google

1. 打开 [Google Search Console](https://search.google.com/search-console)
2. 添加资源 → 输入你的网址 → 按提示验证所有权（GitHub Pages 用 HTML 标签验证最简单）
3. 左侧 **站点地图** → 提交 `sitemap.xml`

### 百度（国内玩家主要来源）

1. 打开 [百度搜索资源平台](https://ziyuan.baidu.com)
2. 添加网站 → 验证所有权
3. **普通收录** → 提交 `sitemap.xml`

> 百度对境外服务器（GitHub Pages / Cloudflare）的收录一向偏慢，可能要几周。如果国内流量是重点，可以考虑国内的静态托管，但那需要备案。

### 加速收录的实际办法

搜索引擎爬虫更看重外部链接。**发布后去 NGA、贴吧、社区发一帖介绍**，效果通常比干等收录快得多。

---

## 后续更新

改完内容之后：

```bash
cd site
git add .
git commit -m "更新说明"
git push
```

GitHub Pages / Cloudflare 会自动重新部署，一两分钟生效。

**补丁更新时记得改三处**：
- `index.html` 顶部的版本徽章和「最后更新」日期
- `index.html` 免责声明里的版本与时效段落
- `sitemap.xml` 里的 `lastmod`

---

## 目录结构

```
site/
├── index.html                首页（介绍 + 索引 + 免责声明）
├── subtlety-rogue.html       敏锐贼      56 题
├── outlaw-rogue.html         狂徒贼      51 题
├── frost-mage.html           冰法        51 题
├── discipline-priest.html    戒律牧      51 题
├── rogue-priest-2v2.html     贼牧 2v2    40 题
├── thug-cleave-3v3.html      Thug Cleave 51 题
├── assets/
│   ├── icons/                171 个技能与职业图标（共享，浏览器缓存一次）
│   └── js/skdb.js            163 个技能的官方描述（共享）
├── sitemap.xml               站点地图
├── robots.txt                爬虫规则
├── set-domain.sh             域名替换脚本
└── .nojekyll                 GitHub Pages 需要，别删
```

全站 1.5 MB。用户打开一个页面实际下载约 83 KB HTML + 首屏可见的图标（图标是懒加载的），**图标缓存后切换页面只有 83 KB**。

---

## 两件我想提醒你的事

### 图标的版权风险是真实的，但可以随时归零

技能图标是暴雪的美术资产。免责声明我按 Fan Content Policy 的标准格式写了，也写明了「权利方可要求移除」——**这是绝大多数攻略站的通行做法，风险很低但不是零**。

如果你想彻底避开，告诉我，我可以把所有图标换成抽象色块或图形符号。体验会打折，但版权问题归零。这个改动大约十几分钟。

### 内容责任变了

私下用错了就错了，公开之后会有人照着练。首页的免责声明里我专门写了一段「**哪些是实测、哪些是推导**」，把两类内容区分开了——尤其是两个组合训练器，公开攻略里几乎没有组合层面的战术展开，那部分的结构基本是我推的，页面里也把推理过程写了出来。

这段建议保留，别删。它既是对读者诚实，也是对你自己的保护。
