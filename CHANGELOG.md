# 更新记录

对应补丁 12.1（Midnight 赛季一）。

## 2026-08-14 · 路线图掉头：坦克不做，治疗插队

进度分母从 40 改成 34，README 从手写改成构建产出。

**6 个坦克专精标成「不做」，不再计入分母。** 竞技场没有坦克位——给不存在的
用户做训练器，做多少都是在给进度条挂一笔永远不会清的账。注册表新增 `skip`
字段，写了理由就是不做；跟 `page: null`（还没做）分开。首页矩阵、路线图、
许愿池的待做清单一起跟着走。

**下一批改成治疗优先。** 之前路线图按 Icy Veins 梯队表从 S 往下推，而那张表
只给 DPS 排名，治疗和坦克在表里全是 `null`——照它排序，治疗永远轮不到。可
2v2/3v3 必带治疗，路线图自己推荐的下三组（RMD / RPS / RLS）分别要恢复德、
恢复萨、暗牧，一个前置件都没有。组合轴卡在这儿，不是卡在没时间。

**许愿池不再因为没配邮件 key 就藏起来。** 之前 `WISH_KEY` 为空时整个入口不
渲染，等于一条信号都收不到；而 GitHub 那条路本来就是通的，只是多一步登录。
现在没 key 时直接开一个填好标题与正文的 Issue，用户点提交就行。
纯静态、无埋点是对的，但那意味着许愿池是这站唯一的信号源——不能关着。

**README 的会过期部分改成构建产出。** 训练器表、题量、进度数字、补丁号原本
全靠手写，连着十几次提交没回写，线上写着「专精 4/40」而实际是 14/34。
新增 `_build/readme.js`，只替换 `<!-- AUTO:xxx -->` 标记之间的内容。
「记得回写 README」是纪律，纪律会被忘；构建产出不会。

## 2026-08-14 · 九职业覆盖完成，新增 RMP 3v3

专精从 5 个做到 14 个，九大职业各有一个可用的训练器；组合从 2 个到 3 个。

新增：踏风武僧、惩戒骑、增强萨、平衡德、湮灭唤魔师、射击猎人、痛苦术士、
浩劫 DH，八个专精六轴一次做齐，各 51 题。RMP 3v3 是第三个组合训练器。

顺手焊进构建自检的两条：
- **排版标准**——新增页面照着已经跑偏的页面抄，页面照样渲染得出来，
  只有跟标准逐字段比才发现
- **空段**——导航承诺一段，正文就得有那一段。Thug Cleave 的「击杀链」曾经
  只有一句导语加一个空容器，构建全绿，用户点进去是空白。现在空段直接失败

## 2026-08-13 · 升到 12.1，新增邪恶死骑

版本号收进 `_build/config.js`，全站（页头徽章、SEO、数据出处、首页、路线图）
跟着一处走，不再往各页写死。

**梯队榜的版本跟补丁号分开。** 梯队表是第三方按自己的节奏更新的，跟游戏补丁
不是一回事；跟着 `PATCH` 走会让页面声称「12.1 的梯队」而数据其实是上个版本
的。新增 `TIER_PATCH`，只有真去重抓了榜单才动那一行。

- 数据更新到 12.1，修掉两处数据源理解错误
- 技能描述标清 PvE / PvP 语境（同一个技能两边时长不同），修致盲时长
- 武器战士补齐题库 51 题，题库结构纳入构建自检
- 新增邪恶死骑，六个轴一次做齐

## 2026-08-12 · 新增专精：武器战士（数据层）

第五个训练器上线。这一版只做**数据层**——天赋树与属性优先级，两块都是实测。
骨架只写数据能支撑的部分，对阵与题库标「待补充」。

**这个专精有一处跟前四个都不同**：英雄天赋是 Slayer 29/50 vs Colossus 21/50，
**58 比 42，top50 内部也没有共识**。前四个专精的英雄天赋都是一边倒
（50/0、46/4、45/5），跟着点就行；武器战士这一格得自己定。

- 天赋树 104 格（其中英雄天赋 28 格属于「选哪条线」这一个决策，不算逐格分歧）；
  职业树与专精树 76 格里 67 格定死，9 格需要判断
- 属性：全能 100 一枝独秀，急速 49 跟上，精通与爆击近乎为零
- PvP 天赋：打磨利刃 49/50、捍卫 34/50、缴械 31/50、Berserker Roar 29/50，
  50×3=150 与实测总和自洽
- 114 条描述译成中文；33 个技能名过 Wowhead 双语回验后使用官方译名
- 76 张图标下载到本地

顺手修的：题库为空的页面不再显示「— 道判断题」和「正确率 —」两个空徽章。

## 2026-08-12 · 天赋树补齐四个专精

狂徒贼、冰法、戒律牧的天赋树全部上线，四个已上线专精的天赋轴至此完整。

| 专精 | 格数 | 定死 | 需要判断 |
|---|---|---|---|
| 敏锐贼 | 113 | 106 | 7 |
| 狂徒贼 | 110 | 93 | 7 |
| 冰法 | 108 | 102 | **3** |
| 戒律牧 | 117 | 86 | **17**（列了最值得想的 7 格） |

**冰法和戒律牧是两个极端**：冰法几乎无从选择，戒律牧的配点空间最大。
这个差异本身就是信息——它决定了你在这个专精上要花多少心思在配点上。

- 367 条描述译成中文，技能名保留英文，附英文原文可切换
- 199 张图标下载到本地（6 张 zamimg 没有的从 render.worldofwarcraft.com 补齐）
- 英雄天赋有两条线的，未选中的那条整条排除 —— 它是「选哪条线」这**一个**
  决策的产物，不是十四个独立分歧点

**修正狂徒贼两处过时数据**（原为 8 人小样本，现在是 50 人）
- 英雄天赋原写「命运之缚 8/8 全用」，实测 Trickster 46/50、命运之缚仅 4 人，结论是反的
- PvP 天赋原写「卸除武装 8/8 必带」，实测 31/50；「先发制人 5/8」实测 38/50 反而更高

## 2026-08-11 · 导入串与天赋浮窗（敏锐贼）

**导入串**放在天赋轴最前面，可一键复制。

但它是**一个人的配点**，不是 top50 的平均——平均配点没有对应的串。
所以标明了来源，并写清它在 7 个分歧格里有 3 格跟多数派不一样：
粘进游戏当起点，那几格再按自己的局面调。

**天赋图标可点**，弹浮窗显示效果描述与「top50 里多少人点」。
分歧格的浮窗里多一个入口，直接跳到对应的判断。

- 122 个天赋描述取自 Murlok.io（英文原文），单独成文件，
  点第一格时才加载——不进首屏
- 复制走三级兜底：clipboard API → execCommand → 都不行就选中串让用户自己复制。
  前两条都要求页面有焦点，不能假定一定可用

## 2026-08-11 · 天赋树热力图（敏锐贼）

天赋树画出来了——124 格按游戏里的树形排布，每格一个图标 + top50 里多少人点。

三种底色一眼分开：**人人都点**（紫圈）、**没人点**（暗）、**有分歧**（灰紫）。
在这之上，**7 个需要判断的格子用橙圈标出来，点一下跳到对应的判断**。

这是跟 Murlok 的区别：它告诉你别人怎么点的，我们还告诉你**哪几格是你要自己想的**。

- 布局数据（grid 定位 + 使用率）由 `_build/tree/_parse.py` 从 Murlok heatmap 解析，
  与 `pages.js` 里的判断层分开存——重抓数据不动判断
- 110 个天赋图标下载到本地（CDN 国内不通，站内既定做法）
- 分歧格可键盘聚焦与 Enter 触发，不只是能点

## 2026-08-11 · 天赋树（敏锐贼）

天赋轴补上职业树 / 专精树。做法不是给一份导入串，是**指出哪几格需要判断**。

实测分布说明了为什么：职业树 44 格 + 专精树 41 格 + 英雄天赋 28 格共 113 格，
top50 里 **106 格是定死的**——要么人人都点，要么没人点。真正要判断的只有 7 格。
抄一份串没有意义，那 7 格才是全部内容。

- 7 个分歧点逐个给：效果 + 使用率（x/50）+ 什么局面下点
- 使用率是实测，「什么时候点」是编者按天赋效果与打法推导，页面上分开标注
- 天赋名查 Wowhead 官方接口核对（Shiv = 毒刃，tooltip 与英文描述逐句对上）；
  查不到的用英文原名

**修正一处既有错误**：PvP 天赋原写「第三格看对面：卸除武装 / 偷天换日」。
实测 50 人 × 3 格 = 150 个选择，分布为烟雾弹 50、Preemptive Maneuver 45、
卸除武装 29、Thick as Thieves 23，四项占 97%。
所以是**三选二**不是三选一；且 PvP 天赋是 Thick as Thieves，
偷天换日是职业树天赋——原文把两者混为一谈。

## 2026-08-11 · 属性优先级（六个训练器）

六个已上线训练器的装备轴补齐。四个专精给实测排序，两个组合页指向成员专精
——属性跟着专精走，队友是谁不改变副属性的收益，组合层面没有额外取舍。

数据取自 Murlok.io 四大区 3v3 各专精 top 50 实测配装。

| 专精 | 排序 | 形状 |
|---|---|---|
| 敏锐贼 | 精通 · 全能 > 急速 > 爆击 | 两条主堆，之后断崖 |
| 狂徒贼 | 全能 > 急速 > 爆击 · 精通 | 全能一枝独秀 |
| 冰法 | 全能 > 急速 > 精通 · 爆击 | 两条主力，后两条归零（爆击为零） |
| 戒律牧 | 精通 > 全能 > 急速 > 爆击 | 三条都有分量，只弃爆击 |

- 条形长度按各属性平均评级值的相对比例，分「主堆 / 兼顾 / 次要 / 可弃」
  四档并在视觉上拉开，让断层位置一眼可见——
  属性优先级的价值在断层在哪，不在四个名次
- 按站内纪律不写绝对点数：装等每赛季在变，相对关系才稳定
- 版面按信息层级收：属性四行合成一张紧凑卡（不是四张独立卡片），
  解读与出处降为正文小字（不再各占一个带边框的框），
  「四个副属性各自意味着什么」是全站共用的科普、每页重复，收进装备通则折叠区不再常驻
- 口径核对：Murlok 同时给了百分比和平均评级值两组数字，
  四个专精交叉验证后确认它自己的 Stat priority 排序按平均评级值排（百分比 4 个里 3 个对不上），
  百分比口径不明，不采用

## 2026-08-11

**更名与定位**
- 站名定为「WOW打架营地」，定位「魔兽世界竞技场 PvP 判断训练」
- 首页标题从情绪式标语换成报站名 + 报定位 + 报版本
- 站名、定位、域名收进 `_build/config.js`，改一处全站生效

**竞技场组合（原来只有两张卡平铺）**
- 按 3v3 / 2v2 分段。这是社区既有的分法：3v3 有公认命名的固定组合，2v2 只是「输出 + 治疗」配对
- 3v3 收录 19 组有名字的组合，2v2 收录 14 组配对，双输出配对单独标记
- 点职业图标筛出该职业能打的组合，再点一次取消
- 清单来源：Icy Veins 各专精 Best Arena Compositions、wowmeta 组合梯队

**架构重构：单体页 → 内容 / 构建 / 静态页三层**
- 原本 6 个各存一份 CSS 与引擎的单体页（各 135KB）
- CSS、引擎、技能库改为全站共享，带内容哈希，缓存一次管所有页
- 正文在构建期内联进 HTML，不依赖 JS 注入
- 题库拆出按需加载，单页首屏 133KB → 81KB
- 加一个专精 = 复制模板 + 加一条配置 + 注册表点亮 + 跑一次构建

**新增内容轴**
- 天赋：已实测的定盘项（英雄天赋、PvP 天赋格）
- 装备与属性：全站共用的 PvP 装备通则；逐专精的属性排序待实测

**文案**
- 全站改为陈述式，删除编辑方针自白与界面说明
- 判据：这句话删掉，读者会不会少知道一件事？不会就删

**修复**
- 组合页题库按 `q.w`（视角）分类、筛选器是 `fWho`，与专精页的 `q.t` / `fCat` 是两套；
  合并引擎时一刀切导致 2v2 与 3v3 的题库抛异常打不开
- 亮色模式下潜行者黄（#FFF468）、牧师白（#FFFFFF）在白底上不可读，
  职业色改由 CSS 变量传递并在亮色模式混入暗色，对比度提到 4.7:1 以上
- 3v3 角色分配器默认分配错误，一进页面就报警告
- 职业筛选点击时布局位移：选中态改了按钮尺寸、按钮靠 flex 自动排列、
  「清除」按钮出现消失——三个来源逐个掐掉，改为等宽网格
- 共享资源加内容哈希，回访用户不会拿到缓存里的旧脚本

## 2026-08-10

- 首次上线，GitHub Pages 部署
- 四个专精训练器（敏锐贼 56 题 / 狂徒贼 51 / 冰法 51 / 戒律牧 51）
- 两个组合训练器（贼牧 2v2 40 题 / Thug Cleave 3v3 51 题）
- 合计 300 道情境判断题
- 技能中文名与图标从 Wowhead 官方接口逐个核对，图标全部下载到本地
  （zamimg CDN 在国内不通，直接引 CDN 手机上是一片空白）
- 题目选项运行时打乱——首版 240 道题里 95% 的正解是 B
- SEO：canonical、Open Graph、JSON-LD、sitemap、robots
